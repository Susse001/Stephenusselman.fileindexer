{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (clang + lldb)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/file_indexer.exe",
            "args": ["D:/", "--ext", ".ext", "--name", "Unity", "--limit", "10"],
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal"
        }
    ]
}